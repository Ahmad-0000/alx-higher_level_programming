-- This is a script to list the attributes "score" and "name" of the
-- table "second_table" in the current database in descending order
-- regarding "score"

SELECT score, name
FROM second_table
ORDER BY score DESC;
